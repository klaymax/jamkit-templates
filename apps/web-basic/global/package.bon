{
    id: "${APP_ID}",
    language: "${LANGUAGE}",
    version: "${VERSION}",
    title: "${APP_TITLE}",
    description: "${APP_DESCRIPTION}",
    localization: {
        ko: {
            title: "${KO_APP_TITLE}",
            description: "${KO_APP_DESCRIPTION}"
        }
    }
}
